"use client";

// Stub implementation - toast functionality disabled until sonner and next-themes are installed
const Toaster = ({ ...props }) => {
  return (
    <div {...props}>
      {/* Sonner toaster stub - requires sonner and next-themes packages */}
    </div>
  );
};

export { Toaster };
